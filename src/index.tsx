import React from 'react';
import ReactDOM from 'react-dom';

import App from './App';

ReactDOM.render(
  <App />, //Componente no React
  document.getElementById('root') //procura a div 'root' no index.html
);
